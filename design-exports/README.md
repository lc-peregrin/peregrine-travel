# Design exports

Anything exported from Claude Design lands here, awaiting integration into `/site` by Claude Code.

Empty right now — no Claude Design session has been run yet. First candidate: a real logo asset
(see the gap flagged in `../docs/BRAND.md`) — the site currently only has a hand-coded inline SVG
wing mark, no exported logo file exists anywhere.
